# SEU Prediction Project — Python dependencies
# Install on HPC: pip install -r requirements.txt --user
# Or in a conda env: conda create -n seu python=3.11 && conda activate seu

# Core
numpy>=1.26
pandas>=2.2
pyarrow>=15.0        # parquet I/O

# ML
torch>=2.2           # LSTM model (install GPU wheel separately via pytorch.org)
scikit-learn>=1.4    # RobustScaler, metrics
joblib>=1.3          # scaler persistence

# Data fetching
requests>=2.31

# NetCDF (for AE9/AP9 RADBELT output)
netCDF4>=1.6

# Optional — accelerates parquet on HPC
# fastparquet>=2024.2
